import { fetch as fetchSetToRouter } from './fetch';
import {
  QueryParameters as SetToRouterQueryParameters,
  Response as SetToRouterResponse,
} from './types';

export type { SetToRouterQueryParameters, SetToRouterResponse };

export { fetchSetToRouter };
