import { fetch as fetchWriteToRouter } from './fetch';
import {
  QueryParameters as WriteToRouterQueryParameters,
  Response as WriteToRouterResponse,
} from './types';

export type { WriteToRouterQueryParameters, WriteToRouterResponse };

export { fetchWriteToRouter };
